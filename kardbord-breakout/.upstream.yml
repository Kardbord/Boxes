# Dummy flatpak manifest for FEDC to check for upstream updates.
id: kardbord-breakout
modules:
  - name: kardbord-breakout
    sources:
      - type: git
        url: https://github.com/Kardbord/breakout.git
        tag: v1.0.3
        commit: 3f51a1fbee9339e850d962ee26a888f3584662e8
        x-checker-data:
          type: git
          tag-pattern: "^v([\\d.]+)$"
